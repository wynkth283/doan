<%= form_with(model: loai_quyen) do |form| %>
  <% if loai_quyen.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(loai_quyen.errors.count, "error") %> prohibited this loai_quyen from being saved:</h2>

      <ul>
        <% loai_quyen.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :TenQuyen, style: "display: block" %>
    <%= form.text_field :TenQuyen %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
